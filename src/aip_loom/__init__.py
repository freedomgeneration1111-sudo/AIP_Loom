"""AIP_Loom — Local-first Python CLI workbench for longform AI document continuity.

This package provides the ``aip-loom`` command-line tool.  The public API
surface is intentionally small: downstream code should import the shared
envelope, error taxonomy, and renderer from the named sub-modules rather
than from this top-level ``__init__``.
"""

__version__ = "0.1.0"
__product__ = "AIP_Loom"
